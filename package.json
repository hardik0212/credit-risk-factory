{
  "name": "credit-risk-factory",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "frontend": "cd frontend && npm run dev -- --port 5173",
    "backend": "cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000"
  }
}
